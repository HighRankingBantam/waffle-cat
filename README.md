# Waffle Cat Classic

Fork of [OldJobobo/waffle-cat](https://github.com/OldJobobo/waffle-cat) pinned to **`alpha-final` (`dd1bd2b`)** — the last commit **before Waffle Cat 2.0**.

2.0 lives on upstream `master`. This fork stays on the classic palette (`#ffbe55` honey gold on `#292025`).

## What’s here

- The original colorscheme repo at `alpha-final` (`colors/`, `palette/`, `configs/`, neovim, generators)
- `omarchy/` — local Omarchy theme snapshot merged in (Hyprland, GTK, terminals, Vencord, **24 wallpapers**: classic 18 + the six from 2.0)

Accent: `#ffbe55`. Background: `#292025`.
